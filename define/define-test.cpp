#include <iostream>

using namespace std;

#define REFLECT_RATIO 1.3

int main() {
    ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    string str = "REFLECT_RATIO : " + REFLECT_RATIO;
    return 0;
}